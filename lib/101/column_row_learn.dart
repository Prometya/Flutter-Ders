import 'package:flutter/material.dart';

class ColumnRownLearnView extends StatelessWidget {
  const ColumnRownLearnView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('Column Row'),
        ),
        body: Column(
          children: <Widget>[],
        ));
  }
}
